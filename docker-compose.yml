version: '3.8'

services:
  api:
    build: ./Api
    ports:
      - "8000:8000"
    volumes:
      - ./Machine-Learning/models:/Machine-Learning/models

  web:
    build: ./web/app
    ports:
      - "8001:8001"
    depends_on:
      - api
    env_file:
      - ./web/app/.env